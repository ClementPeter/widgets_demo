import 'package:flutter/material.dart';

class BtmLocation extends StatelessWidget {
  const BtmLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 100,
      child: Card(
        child: Center(
          child: Text('Location UI'),
        ),
      ),
    );
  }
}
